## Usage

Go to nyaa, set your filters, make sure the team is first, automatically tries in alphabetical order by adding a space behind the first search term and then the alphabetic character, make sure to end the url with &page=1, wrap the url in quotation
`node . "{url}"`
